# espanso match file
# Author: JeansenVaars
# Consider inviting me a Coffee, this took quite some work! -> https://ko-fi.com/JeansenVaars
# Check out my Blog -> https://jvhouse.xyz/
# USE AT YOUR OWN RISK

matches:
  # GUM - GM Question
  - trigger: ":gqgm"
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - table
            - --table
            - gum_qqgm
  # GUM - GM Question
  - trigger: ":+gqgm"
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - table
            - --table
            - gum_qqgm
            - --mode
            - adv
  # GUM - GM Question
  - trigger: ":-gqgm"
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - table
            - --table
            - gum_qqgm
            - --mode
            - dis
  # GUM - PC Question
  - trigger: ":gqpc"
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - table
            - --table
            - gum_qqpc
  # GUM - PC Question
  - trigger: ":+gqpc"
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - table
            - --table
            - gum_qqpc
            - --mode
            - adv
  # GUM - PC Question
  - trigger: ":-gqpc"
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - table
            - --table
            - gum_qqpc
            - --mode
            - dis
  # GUM - NPC Question
  - trigger: ":gqnpc"
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - table
            - --table
            - gum_qqnpc
  # GUM - NPC Question
  - trigger: ":+gqnpc"
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - table
            - --table
            - gum_qqnpc
  # GUM - NPC Question
  - trigger: ":-gqnpc"
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - table
            - --table
            - gum_qqnpc
  # GUM - Grand oracle basic
  - triggers: [":gob"]
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - table
            - --table
            - gum_go_act,gum_go_su
  # GUM - Grand oracle rich
  - triggers: [":gor"]
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - table
            - --table
            - gum_go_act,gum_go_desc,gum_go_su
  # GUM - Grand oracle subject
  - triggers: [":gos"]
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - table
            - --table
            - gum_go_desc,gum_go_su
  # GUM - Grand oracle description
  - triggers: [":god"]
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - table
            - --table
            - gum_go_desc,gum_go_desc
  # GUM - Next Scene prompt Meta-Tool
  - triggers: [":gscene"]
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - table
            - --table
            - gum_scene
  # Challenge Skill Test
  - triggers: [":gskill"]
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - table
            - --table
            - gum_challengea,gum_challengeb
  # Combat conflict random
  - triggers: [":gcombat", ":gconflict"]
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - table
            - --table
            - gum_combat
  # Plan check - Safe
  - trigger: ":gpc"
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - table
            - --table
            - gum_pch_safe
  # Plan check - Risky
  - trigger: ":+gpc"
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - table
            - --table
            - gum_pch_risky
  # Plan check - Tense
  - trigger: ":++gpc"
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - table
            - --table
            - gum_pch_tense
  # Plan check - Exploding
  - trigger: ":+++gpc"
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - table
            - --table
            - gum_pch_exploding
  # GM Intervention
  - trigger: ":ginterv"
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - table
            - --table
            - gum_gm_intervention
  # Locations identity
  - trigger: ":glocid"
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - table
            - --table
            - gum_locid
  # Locations detail
  - trigger: ":glocdet"
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - table
            - --table
            - gum_locdet
  # NPC identity
  - trigger: ":gnpcid"
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - table
            - --table
            - gum_npcid
  # NPC detail
  - trigger: ":gnpcdet"
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - table
            - --table
            - gum_npcdet
  # Motive good
  - trigger: ":gmotivegood"
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - table
            - --table
            - gum_motiv_good
  # Motive evil
  - trigger: ":gmotiveevil"
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - table
            - --table
            - gum_motiv_evil
  # Action good
  - trigger: ":gactgood"
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - table
            - --table
            - gum_act_good
  # Action evil
  - trigger: ":gactevil"
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - table
            - --table
            - gum_act_evil
  # NPC - Who?
  - triggers: [":gwho", ":gnpcwho"]
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - table
            - --table
            - gum_npc_who
  # Setup location
  - trigger: ":gsetlocation"
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - table
            - --table
            - gum_set_loc
  # Setup problem
  - trigger: ":gsetproblem"
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - table
            - --table
            - gum_set_problem
  # Setup hook
  - trigger: ":gsethook"
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - table
            - --table
            - gum_set_hook
  # Setup mission
  - trigger: ":gsetmission"
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - table
            - --table
            - gum_set_misa,gum_set_misb
  # Setup consider
  - trigger: ":gsetconsider"
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - table
            - --table
            - gum_set_consider
  # Setup lead
  - trigger: ":gsetlead"
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - table
            - --table
            - gum_set_lead
  # Setup full guide
  - trigger: ":gsetfull"
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - table
            - --table
            - gum_set_full
  # GUM-e prober world
  - trigger: ":gprobew"
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - table
            - --table
            - gume_prober_world
  # GUM-e prober scene
  - trigger: ":gprobes"
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - table
            - --table
            - gume_prober_scene
  # GUM-e discovery
  - trigger: ":gdisc"
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - table
            - --table
            - gume_discovery
  # GUM-e discovery past
  - trigger: ":gpast"
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - table
            - --table
            - gume_discovery_past
  # GUM-e discovery future
  - trigger: ":gfuture"
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - table
            - --table
            - gume_discovery_future
  # GUM-e discovery clues
  - triggers: [":gclue", ":ginfo"]
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - table
            - --table
            - gume_discovery
  # GUM-e discovery reasons
  - trigger: ":greason"
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - table
            - --table
            - gume_discovery_reason
  # GUM-e interaction
  - trigger: ":ginteract"
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - table
            - --table
            - gume_interaction
  # GUM-e interaction behavior
  - trigger: ":gbehav"
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - table
            - --table
            - gume_interaction_behavior
  # GUM-e interaction contribution
  - trigger: ":gcontrib"
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - table
            - --table
            - gume_interaction_contribution
  # GUM-e interaction response
  - trigger: ":gresponse"
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - table
            - --table
            - gume_interaction_response
  # GUM-e interaction request
  - trigger: ":grequest"
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - table
            - --table
            - gume_interaction_request
  # GUM-e enemy actions
  - trigger: ":genemy"
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - table
            - --table
            - gume_enemy
  # GUM-e enemy insights
  - triggers: [":gfeel", ":ginsight"]
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - table
            - --table
            - gume_enemy_feelings
  # GUM-e enemy risk
  - trigger: ":grisk"
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - table
            - --table
            - gume_enemy_risks
  # GUM-e enemy event
  - trigger: ":gevent"
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - table
            - --table
            - gume_enemy_events
  # GUM-e enemy threat
  - trigger: ":gthreat"
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - table
            - --table
            - gume_enemy_threats
