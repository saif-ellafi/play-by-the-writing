# espanso match file
# Author: JeansenVaars
# Consider inviting me a Coffee, this took quite some work! -> https://ko-fi.com/JeansenVaars
# Check out my Blog -> https://jvhouse.xyz/
# USE AT YOUR OWN RISK

matches:
  # PUM
  - trigger: ":qq"
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - table
            - --table
            - pum_question_even,pum_question_caveat
  # PUM
  - trigger: ":+qq"
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - table
            - --table
            - pum_question_likely,pum_question_caveat
  # PUM
  - trigger: ":-qq"
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - table
            - --table
            - pum_question_unlikely,pum_question_caveat
  # Next Scene prompt - PUM
  - triggers: [":scene", ":prompt"]
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - table
            - --table
            - pum_prompt
  # Next Scene skill
  - triggers: [":challenge", ":skill"]
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - table
            - --table
            - pum_challenge
  # Next Scene High Stakes
  - trigger: ":discovery"
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - table
            - --table
            - pum_discovery
  # Next Scene High Stakes
  - trigger: ":risk"
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - table
            - --table
            - pum_risk
  # PUM Scene Checker
  - triggers: [":check", ":expectation"]
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - table
            - --table
            - pum_check
  # PUM
  - trigger: ":item"
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - table
            - --table
            - pum_kindi,pum_kindi
  # PUM
  - trigger: ":ability"
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - table
            - --table
            - pum_kinda,pum_kinda
  # PUM
  - trigger: ":role"
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - table
            - --table
            - pum_kindr,pum_kindr
  # PUM
  - trigger: ":enemy"
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - table
            - --table
            - pum_kinde,pum_kinde
  # PUM
  - trigger: ":danger"
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - table
            - --table
            - pum_kindd,pum_kindd
  # PUM
  - triggers: [":area", ":location"]
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - table
            - --table
            - pum_looks_adv,pum_looks_area_adj
  # PUM
  - trigger: ":someone"
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - table
            - --table
            - pum_looks_adv,pum_looks_npc_adj
  # PUM
  - trigger: ":object"
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - table
            - --table
            - pum_looks_adv,pum_looks_obj_adj
  # PUM
  - trigger: ":what"
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - table
            - --table
            - pum_whata,pum_whatb
  # PUM
  - trigger: ":catalyst"
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - table
            - --table
            - pum_catalyst
  # PUM
  - trigger: ":subject"
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - table
            - --table
            - pum_subject
  # PUM
  - trigger: ":who"
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - table
            - --table
            - pum_whoa,pum_whob
  # PUM
  - trigger: ":intent"
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - table
            - --table
            - pum_inta,pum_intb
  # PUM
  - trigger: ":activity"
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - table
            - --table
            - pum_acta,pum_actb
  # PUM
  - trigger: ":reason"
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - table
            - --table
            - pum_reasona,pum_reasonb
  # Twist or Complication
  - trigger: ":complication"
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - table
            - --table
            - pum_subject,pum_complication
  # Circumstances.
  - trigger: ":circumstance"
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - table
            - --table
            - pum_circumstance
  # SUM
  - trigger: ":gma"
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - table
            - --table
            - sum_gm_action
  # SUM
  - trigger: ":+gma"
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - table
            - --table
            - sum_gm_action
            - --mode
            - adv
  # SUM
  - trigger: ":-gma"
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - table
            - --table
            - sum_gm_action
            - --mode
            - dis
  # SUM
  - trigger: ":gmf"
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - table
            - --table
            - sum_gm_feedback
  # SUM
  - trigger: ":+gmf"
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - table
            - --table
            - sum_gm_feedback
            - --mode
            - adv
  # SUM
  - trigger: ":-gmf"
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - table
            - --table
            - sum_gm_feedback
            - --mode
            - dis
  # SUM
  - trigger: ":gmw"
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - table
            - --table
            - sum_gm_world
  # SUM
  - trigger: ":npcc"
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - table
            - --table
            - sum_npc_contribution
  # SUM
  - trigger: ":+npcc"
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - table
            - --table
            - sum_npc_contribution
            - --mode
            - adv
  # SUM
  - trigger: ":-npcc"
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - table
            - --table
            - sum_npc_contribution
            - --mode
            - dis
  # SUM
  - trigger: ":npcb"
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - table
            - --table
            - sum_npc_behavior
  # SUM
  - trigger: ":+npcb"
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - table
            - --table
            - sum_npc_behavior
            - --mode
            - adv
  # SUM
  - trigger: ":-npcb"
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - table
            - --table
            - sum_npc_behavior
            - --mode
            - dis
  # SUM
  - trigger: ":npco"
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - table
            - --table
            - sum_npc_opinion
  # SUM
  - trigger: ":+npco"
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - table
            - --table
            - sum_npc_opinion
            - --mode
            - adv
  # SUM
  - trigger: ":-npco"
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - table
            - --table
            - sum_npc_opinion
            - --mode
            - dis
  # SUM
  - trigger: ":npca"
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - table
            - --table
            - sum_npc_answer
  # SUM
  - trigger: ":+npca"
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - table
            - --table
            - sum_npc_answer
            - --mode
            - adv
  # SUM
  - trigger: ":-npca"
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - table
            - --table
            - sum_npc_answer
            - --mode
            - dis
  # SUM (Action)
  - trigger: ":sumac"
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - table
            - --table
            - sum_action
  # SUM (Subject)
  - trigger: ":sumsu"
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - table
            - --table
            - sum_subject
  # SUM (Adjective)
  - trigger: ":sumad"
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - table
            - --table
            - sum_adjective