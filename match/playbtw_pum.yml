# espanso match file
# Author: JeansenVaars
# Consider inviting me a Coffee, this took quite some work! -> https://ko-fi.com/JeansenVaars
# Check out my Blog -> https://jvhouse.xyz/
# USE AT YOUR OWN RISK

matches:
  # PUM Question: deterministic
  - trigger: ":qq"
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - table
            - --table
            - pum_question_deterministic
  # PUM Question: deterministic
  - trigger: ":+qq"
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - table
            - --table
            - pum_question_deterministic
            - --mode
            - adv
  # PUM Question: deterministic
  - trigger: ":-qq"
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - table
            - --table
            - pum_question_deterministic
            - --mode
            - dis
  # PUM Question: subjective
  - trigger: ":qsub"
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - table
            - --table
            - pum_question_subjective
  # PUM Question: subjective
  - trigger: ":+qsub"
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - table
            - --table
            - pum_question_subjective
            - --mode
            - adv
  # PUM Question: subjective
  - trigger: ":-qsub"
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - table
            - --table
            - pum_question_subjective
            - --mode
            - dis
  # PUM Question: interaction
  - trigger: ":qint"
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - table
            - --table
            - pum_question_interaction
  # PUM Question: interaction
  - trigger: ":+qint"
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - table
            - --table
            - pum_question_interaction
            - --mode
            - adv
  # PUM Question: interaction
  - trigger: ":-qint"
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - table
            - --table
            - pum_question_interaction
            - --mode
            - dis
  # PUM - Who
  - trigger: ":who"
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - table
            - --table
            - pum_who
  # PUM - Intent
  - trigger: ":intent"
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - table
            - --table
            - pum_intent
  # PUM - Activity
  - trigger: ":activity"
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - table
            - --table
            - pum_activity
  # PUM - Place
  - triggers: [":area", ":location", ":place"]
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - table
            - --table
            - pum_place
  # PUM - Object
  - trigger: ":object"
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - table
            - --table
            - pum_object
  # PUM - Ability
  - trigger: ":ability"
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - table
            - --table
            - pum_ability
  # PUM - Subject
  - trigger: ":subject"
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - table
            - --table
            - pum_subject
  # PUM - Scene Random Prompt
  - triggers: [":scene", ":prompt"]
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - table
            - --table
            - pum_prompt
  # PUM - Scene Modified Proposal
  - triggers: [":check", ":expectation", ":proposal"]
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - table
            - --table
            - pum_proposal
  # PUM - Complication
  - trigger: ":complication"
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - table
            - --table
            - pum_complication
  # PUM - Catalyst
  - trigger: ":catalyst"
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - table
            - --table
            - pum_catalyst
  # PUM - Challenge
  - triggers: [":challenge", ":skill"]
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - table
            - --table
            - pum_challenge
  # PUM - Situation
  - trigger: ":situation"
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - table
            - --table
            - pum_situation
  # SUM (GM action, intervention)
  - trigger: ":gma"
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - table
            - --table
            - sum_gm_action
  # SUM (GM action, intervention)
  - trigger: ":+gma"
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - table
            - --table
            - sum_gm_action
            - --mode
            - adv
  # SUM (GM action, intervention)
  - trigger: ":-gma"
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - table
            - --table
            - sum_gm_action
            - --mode
            - dis
  # SUM (GM feedback, reaction)
  - trigger: ":gmf"
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - table
            - --table
            - sum_gm_feedback
  # SUM (GM feedback, reaction)
  - trigger: ":+gmf"
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - table
            - --table
            - sum_gm_feedback
            - --mode
            - adv
  # SUM (GM feedback, reaction)
  - trigger: ":-gmf"
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - table
            - --table
            - sum_gm_feedback
            - --mode
            - dis
  # SUM (GM World, factions)
  - trigger: ":gmw"
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - table
            - --table
            - sum_gm_world
  # SUM (GM World, factions)
  - trigger: ":+gmw"
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - table
            - --table
            - sum_gm_world
            - --mode
            - adv
  # SUM (GM World, factions)
  - trigger: ":-gmw"
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - table
            - --table
            - sum_gm_world
            - --mode
            - dis
  # SUM (PC matters)
  - trigger: ":pcmatt"
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - table
            - --table
            - sum_pc_matters
  # SUM (PC matters)
  - trigger: ":+pcmatt"
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - table
            - --table
            - sum_pc_matters
            - --mode
            - adv
  # SUM (PC matters)
  - trigger: ":-pcmatt"
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - table
            - --table
            - sum_pc_matters
            - --mode
            - dis
  # SUM (PC backstories)
  - trigger: ":pcback"
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - table
            - --table
            - sum_pc_backs
  # SUM (PC backstories)
  - trigger: ":+pcback"
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - table
            - --table
            - sum_pc_backs
            - --mode
            - adv
  # SUM (PC backstories)
  - trigger: ":-pcback"
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - table
            - --table
            - sum_pc_backs
            - --mode
            - dis
  # SUM (PC bonding)
  - trigger: ":pcbond"
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - table
            - --table
            - sum_pc_bonds
  # SUM (PC bonding)
  - trigger: ":+pcbond"
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - table
            - --table
            - sum_pc_bonds
            - --mode
            - adv
  # SUM (PC bonding)
  - trigger: ":-pcbond"
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - table
            - --table
            - sum_pc_bonds
            - --mode
            - dis
  # SUM (NPC behavior, attitude)
  - trigger: ":npcb"
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - table
            - --table
            - sum_npc_behavior
  # SUM (NPC behavior, attitude)
  - trigger: ":+npcb"
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - table
            - --table
            - sum_npc_behavior
            - --mode
            - adv
  # SUM (NPC behavior, attitude)
  - trigger: ":-npcb"
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - table
            - --table
            - sum_npc_behavior
            - --mode
            - dis
  # SUM (NPC Contribution)
  - trigger: ":npcc"
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - table
            - --table
            - sum_npc_contribution
  # SUM (NPC Contribution)
  - trigger: ":+npcc"
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - table
            - --table
            - sum_npc_contribution
            - --mode
            - adv
  # SUM (NPC Contribution)
  - trigger: ":-npcc"
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - table
            - --table
            - sum_npc_contribution
            - --mode
            - dis
  # SUM (NPC opinion)
  - trigger: ":npco"
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - table
            - --table
            - sum_npc_opinion
  # SUM (NPC opinion)
  - trigger: ":+npco"
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - table
            - --table
            - sum_npc_opinion
            - --mode
            - adv
  # SUM (NPC opinion)
  - trigger: ":-npco"
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - table
            - --table
            - sum_npc_opinion
            - --mode
            - dis
  # SUM (Stereotypes)
  - trigger: ":sumst"
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - table
            - --table
            - sum_stereo
  # SUM (Action)
  - trigger: ":sumac"
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - table
            - --table
            - sum_action
  # SUM (Subject)
  - trigger: ":sumsu"
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - table
            - --table
            - sum_subject
  # SUM (Adjective)
  - trigger: ":sumad"
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - table
            - --table
            - sum_adjective
  # PUM World Aspects
  - triggers: [":worlds", ":aspects"]
    replace: "{{output}}"
    vars:
      - name: entries
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - load_utable
            - --table
            - "pum_nodes_world"
            - --contains
            - "1-2 CHOOSE OR REROLL\n3-4 CHOOSE OR REROLL\n5-6 CHOOSE OR REROLL\n7-8 CHOOSE OR REROLL\n9-10 ADD NEW OR REROLL\n"
      - name: form1
        type: form
        params:
          layout: |
            PUM Plot Nodes (World Aspects):
            [[file1]]
          fields:
            file1:
              type: text
              multiline: true
              default: "{{entries}}"
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - save_utable
            - --table
            - "pum_nodes_world"
            - --contains
            - "{{form1.file1}}"
  - triggers: [":worldr", ":aspectr"]
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - utable
            - --table
            - "pum_nodes_world"
  # PUM Relevant Actors
  - trigger: ":actors"
    replace: "{{output}}"
    vars:
      - name: entries
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - load_utable
            - --table
            - "pum_nodes_actor"
            - --contains
            - "1-2 CHOOSE OR REROLL\n3-4 CHOOSE OR REROLL\n5-6 CHOOSE OR REROLL\n7-8 CHOOSE OR REROLL\n9-10 ADD NEW OR REROLL\n"
      - name: form1
        type: form
        params:
          layout: |
            PUM Plot Nodes (Relevant Actors):
            [[file1]]
          fields:
            file1:
              type: text
              multiline: true
              default: "{{entries}}"
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - save_utable
            - --table
            - "pum_nodes_actor"
            - --contains
            - "{{form1.file1}}"
  - trigger: ":actorr"
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - utable
            - --table
            - "pum_nodes_actor"
  # PUM Story Troubles
  - trigger: ":actors"
    replace: "{{output}}"
    vars:
      - name: entries
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - load_utable
            - --table
            - "pum_nodes_trouble"
            - --contains
            - "1-2 CHOOSE OR REROLL\n3-4 CHOOSE OR REROLL\n5-6 CHOOSE OR REROLL\n7-8 CHOOSE OR REROLL\n9-10 ADD NEW OR REROLL\n"
      - name: form1
        type: form
        params:
          layout: |
            PUM Plot Nodes (Story Troubles):
            [[file1]]
          fields:
            file1:
              type: text
              multiline: true
              default: "{{entries}}"
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - save_utable
            - --table
            - "pum_nodes_trouble"
            - --contains
            - "{{form1.file1}}"
  - trigger: ":actorr"
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - utable
            - --table
            - "pum_nodes_trouble"
  # PUM Active Leads
  - trigger: ":leads"
    replace: "{{output}}"
    vars:
      - name: entries
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - load_utable
            - --table
            - "pum_nodes_lead"
            - --contains
            - "1-2 CHOOSE OR REROLL\n3-4 CHOOSE OR REROLL\n5-6 CHOOSE OR REROLL\n7-8 CHOOSE OR REROLL\n9-10 ADD NEW OR REROLL\n"
      - name: form1
        type: form
        params:
          layout: |
            PUM Plot Nodes (Active Leads):
            [[file1]]
          fields:
            file1:
              type: text
              multiline: true
              default: "{{entries}}"
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - save_utable
            - --table
            - "pum_nodes_lead"
            - --contains
            - "{{form1.file1}}"
  - trigger: ":leadr"
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - utable
            - --table
            - "pum_nodes_lead"