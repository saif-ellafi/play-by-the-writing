# espanso match file
# Author: JeansenVaars
# Consider inviting me a Coffee, this took quite some work! -> https://ko-fi.com/JeansenVaars
# Check out my Blog -> https://jvhouse.xyz/
# USE AT YOUR OWN RISK

matches:
  # GUM V2 - Scene Exploration
  - trigger: ":ggexplo"
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - table
            - --table
            - gum2_exploration
  # GUM V2 - Scene Battle
  - trigger: ":ggbatt"
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - table
            - --table
            - gum2_battle
  # GUM V2 - Plot clue type
  - trigger: ":ggclue"
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - table
            - --table
            - gum2_plot_clue_type
  # GUM V2 - Plot finding
  - trigger: ":ggfind"
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - table
            - --table
            - gum2_plot_finding
  # GUM V2 - Plot enemy activities
  - trigger: ":ggenemyact"
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - table
            - --table
            - gum2_plot_activities
  # GUM V2 - Plot occurrence
  - trigger: ":ggoccur"
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - table
            - --table
            - gum2_plot_occurrence
  # GUM V2 - Nemesis impression
  - trigger: ":ggnemesisimp"
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - table
            - --table
            - gum2_nemesis_impression
  # GUM V2 - Nemesis deeds
  - trigger: ":ggnemesisdeed"
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - table
            - --table
            - gum2_nemesis_deeds
  # GUM V2 - Nemesis intention
  - trigger: ":ggnemesisint"
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - table
            - --table
            - gum2_nemesis_intention
  # GUM V2 - Location feature
  - trigger: ":gglocfeat"
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - table
            - --table
            - gum2_location_feature
  # GUM V2 - Location worth
  - trigger: ":gglocworth"
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - table
            - --table
            - gum2_location_worth
  # GUM V2 - Location purpose
  - trigger: ":gglocpurp"
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - table
            - --table
            - gum2_location_purpose
  # GUM V2 - Location content
  - trigger: ":ggloccont"
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - table
            - --table
            - gum2_location_content
  # GUM V2 - Character by possessions
  - trigger: ":ggcharposs"
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - table
            - --table
            - gum2_char_possessions
  # GUM V2 - Character by impression
  - trigger: ":ggcharimp"
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - table
            - --table
            - gum2_char_impression
  # GUM V2 - Character by activity
  - trigger: ":ggcharact"
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - table
            - --table
            - gum2_char_activity
  # GUM V2 - Character by intention
  - trigger: ":ggcharint"
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - table
            - --table
            - gum2_char_intention
  # GUM V2 - Creature by type
  - trigger: ":ggcretyp"
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - table
            - --table
            - gum2_creature_type
  # GUM V2 - Creature by ability
  - trigger: ":ggcreab"
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - table
            - --table
            - gum2_creature_ability
  # GUM V2 - Creature by behavior
  - trigger: ":ggcrebeh"
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - table
            - --table
            - gum2_creature_behavior
  # GUM V2 - Object by function
  - trigger: ":ggobjfunc"
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - table
            - --table
            - gum2_object_function
  # GUM V2 - Object by form
  - trigger: ":ggobjform"
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - table
            - --table
            - gum2_object_form
  # GUM V2 - Object by state
  - trigger: ":ggobjstate"
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - table
            - --table
            - gum2_object_state
  # GUM V2 - Faction focus
  - trigger: ":ggfacfoc"
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - table
            - --table
            - gum2_faction_focus
  # GUM V2 - Faction resources
  - trigger: ":ggfacres"
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - table
            - --table
            - gum2_faction_resource
  # GUM V2 - Good motives
  - trigger: ":ggmotgood"
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - table
            - --table
            - gum2_good_motivation
  # GUM V2 - Evil motives
  - trigger: ":ggmotevil"
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - table
            - --table
            - gum2_evil_motivation
  # GUM V2 - Good deeds
  - triggers: [":ggdeedsgood", ":gggoodact"]
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - table
            - --table
            - gum2_good_deed
  # GUM V2 - Evil deeds
  - triggers: [":ggdeedsevil", ":ggevilact"]
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - table
            - --table
            - gum2_evil_deed
  # GUM V2 - Grand Oracle (all)
  - trigger: ":ggorac"
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - table
            - --table
            - gum2_grand_oracle
  # GUM V2 - Grand Oracle (action)
  - trigger: ":ggact"
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - table
            - --table
            - gum2_grand_action
  # GUM V2 - Grand Oracle (adjective)
  - trigger: ":ggadj"
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - table
            - --table
            - gum2_grand_adjective
  # GUM V2 - Grand Oracle (subject)
  - trigger: ":ggsub"
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - table
            - --table
            - gum2_grand_subject
  # GUM V2 - World location archetype
  - trigger: ":ggsetloc"
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - table
            - --table
            - gum2_setup_location
  # GUM V2 - World background
  - trigger: ":ggsetback"
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - table
            - --table
            - gum2_setup_background
  # GUM V2 - Campaign goals mission
  - trigger: ":ggsetmission"
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - table
            - --table
            - gum2_setup_mission
  # GUM V2 - Campaign goals opposition
  - trigger: ":ggsetopp"
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - table
            - --table
            - gum2_setup_opposition
  # GUM V2 - Campaign motivation
  - trigger: ":ggsetmot"
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - table
            - --table
            - gum2_setup_motivation
  # GUM V2 - Quest hook
  - trigger: ":ggsethook"
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - table
            - --table
            - gum2_setup_hook
  # GUM V2 - Quest lead
  - trigger: ":ggsetlead"
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - table
            - --table
            - gum2_setup_lead
  # GUM V2 - Quest caveat
  - trigger: ":ggsetcaveat"
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - table
            - --table
            - gum2_setup_caveat
  # GUM V2 Setup full guide
  - trigger: ":ggsetfull"
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - table
            - --table
            - gum2_setup_full