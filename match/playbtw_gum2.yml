# espanso match file
# Author: JeansenVaars
# Consider inviting me a Coffee, this took quite some work! -> https://ko-fi.com/JeansenVaars
# Check out my Blog -> https://jvhouse.xyz/
# USE AT YOUR OWN RISK

matches:
  # GUM V2 - Scene Exploration
  - triggers: [":gum", ":ggexplo"]
    label: "Scene exploration"
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - table
            - --table
            - gum2_exploration
  # GUM V2 - Scene Battle
  - triggers: [":gum", ":ggbatt"]
    label: "Scene battle"
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - table
            - --table
            - gum2_battle
  # GUM V2 - Plot clue type
  - triggers: [":gum", ":ggclue"]
    label: "Plot discovery: Clue type"
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - table
            - --table
            - gum2_plot_clue_type
  # GUM V2 - Plot finding
  - triggers: [":gum", ":ggfind"]
    label: "Plot discovery: Useful finding"
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - table
            - --table
            - gum2_plot_finding
  # GUM V2 - Plot enemy activities
  - triggers: [":gum", ":ggenemyact"]
    label: "Plot discovery: Enemy activities"
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - table
            - --table
            - gum2_plot_activities
  # GUM V2 - Plot occurrence
  - triggers: [":gum", ":ggoccur"]
    label: "Plot discovery: Recent occurrences"
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - table
            - --table
            - gum2_plot_occurrence
  # GUM V2 - Nemesis impression
  - triggers: [":gum", ":ggnemesisimp"]
    label: "Nemesis impression"
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - table
            - --table
            - gum2_nemesis_impression
  # GUM V2 - Nemesis deeds
  - triggers: [":gum", ":ggnemesisdeed"]
    label: "Nemesis deeds"
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - table
            - --table
            - gum2_nemesis_deeds
  # GUM V2 - Nemesis intention
  - triggers: [":gum", ":ggnemesisint"]
    label: "Nemesis intent"
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - table
            - --table
            - gum2_nemesis_intention
  # GUM V2 - Location feature
  - triggers: [":gum", ":gglocfeat"]
    label: "Location by feature"
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - table
            - --table
            - gum2_location_feature
  # GUM V2 - Location worth
  - triggers: [":gum", ":gglocworth"]
    label: "Location by worth"
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - table
            - --table
            - gum2_location_worth
  # GUM V2 - Location purpose
  - triggers: [":gum", ":gglocpurp"]
    label: "Location by purpose"
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - table
            - --table
            - gum2_location_purpose
  # GUM V2 - Location content
  - triggers: [":gum", ":ggloccont"]
    label: "Location by content"
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - table
            - --table
            - gum2_location_content
  # GUM V2 - Character by possessions
  - triggers: [":gum", ":ggcharposs"]
    label: "Character by possessions"
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - table
            - --table
            - gum2_char_possessions
  # GUM V2 - Character by impression
  - triggers: [":gum", ":ggcharimp"]
    label: "Character by impression"
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - table
            - --table
            - gum2_char_impression
  # GUM V2 - Character by activity
  - triggers: [":gum", ":ggcharact"]
    label: "Character by activity"
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - table
            - --table
            - gum2_char_activity
  # GUM V2 - Character by intention
  - triggers: [":gum", ":ggcharint"]
    label: "Character by intention"
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - table
            - --table
            - gum2_char_intention
  # GUM V2 - Creature by type
  - triggers: [":gum", ":ggcretyp"]
    label: "Creature by type"
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - table
            - --table
            - gum2_creature_type
  # GUM V2 - Creature by ability
  - triggers: [":gum", ":ggcreab"]
    label: "Creature by ability"
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - table
            - --table
            - gum2_creature_ability
  # GUM V2 - Creature by behavior
  - triggers: [":gum", ":ggcrebeh"]
    label: "Creature by behavior"
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - table
            - --table
            - gum2_creature_behavior
  # GUM V2 - Object by function
  - triggers: [":gum", ":ggobjfunc"]
    label: "Object by function"
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - table
            - --table
            - gum2_object_function
  # GUM V2 - Object by form
  - triggers: [":gum", ":ggobjform"]
    label: "Object by form"
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - table
            - --table
            - gum2_object_form
  # GUM V2 - Object by state
  - triggers: [":gum", ":ggobjstate"]
    label: "Object by state"
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - table
            - --table
            - gum2_object_state
  # GUM V2 - Faction focus
  - triggers: [":gum", ":ggfacfoc"]
    label: "Faction focus"
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - table
            - --table
            - gum2_faction_focus
  # GUM V2 - Faction resources
  - triggers: [":gum", ":ggfacres"]
    label: "Faction resources"
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - table
            - --table
            - gum2_faction_resource
  # GUM V2 - Good motives
  - triggers: [":gum", ":ggmotgood"]
    label: "Good purposes or motivations"
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - table
            - --table
            - gum2_good_motivation
  # GUM V2 - Evil motives
  - triggers: [":gum", ":ggmotevil"]
    label: "Evil purposes or motivations"
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - table
            - --table
            - gum2_evil_motivation
  # GUM V2 - Good deeds
  - triggers: [":gum", ":ggdeedsgood", ":gggoodact"]
    label: "Good intentions or deeds"
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - table
            - --table
            - gum2_good_deed
  # GUM V2 - Evil deeds
  - triggers: [":gum", ":ggdeedsevil", ":ggevilact"]
    label: "Evil intentions or deeds"
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - table
            - --table
            - gum2_evil_deed
  # GUM V2 - Grand Oracle (all)
  - triggers: [":gum", ":ggorac"]
    label: "Grand oracle (all)"
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - table
            - --table
            - gum2_grand_oracle
  # GUM V2 - Grand Oracle (action)
  - triggers: [":gum", ":ggact"]
    label: "Grand oracle (subject)"
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - table
            - --table
            - gum2_grand_action
  # GUM V2 - Grand Oracle (adjective)
  - triggers: [":gum", ":ggadj"]
    label: "Grand oracle (adjective)"
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - table
            - --table
            - gum2_grand_adjective
  # GUM V2 - Grand Oracle (subject)
  - triggers: [":gum", ":ggsub"]
    label: "Grand oracle (subject)"
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - table
            - --table
            - gum2_grand_subject
  # GUM V2 - World location archetype
  - triggers: [":ggsetloc"]
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - table
            - --table
            - gum2_setup_location
  # GUM V2 - World background
  - triggers: [":ggsetback"]
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - table
            - --table
            - gum2_setup_background
  # GUM V2 - Campaign goals mission
  - triggers: [":ggsetmission"]
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - table
            - --table
            - gum2_setup_mission
  # GUM V2 - Campaign goals opposition
  - triggers: [":ggsetopp"]
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - table
            - --table
            - gum2_setup_opposition
  # GUM V2 - Campaign motivation
  - triggers: [":ggsetmot"]
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - table
            - --table
            - gum2_setup_motivation
  # GUM V2 - Quest hook
  - triggers: [":ggsethook"]
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - table
            - --table
            - gum2_setup_hook
  # GUM V2 - Quest lead
  - triggers: [":ggsetlead"]
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - table
            - --table
            - gum2_setup_lead
  # GUM V2 - Quest caveat
  - triggers: [":ggsetcaveat"]
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - table
            - --table
            - gum2_setup_caveat
  # GUM V2 Setup guide
  - triggers: [":gum", ":ggsetfull"]
    label: "Plot seed (full)"
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - table
            - --table
            - gum2_setup_full
  - triggers: [":gum"]
    label: "Plot seed: Caveat"
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - table
            - --table
            - gum2_setup_caveat
  - triggers: [":gum"]
    label: "Plot seed: Hook"
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - table
            - --table
            - gum2_setup_hook
  - triggers: [":gum"]
    label: "Plot seed: Initial lead"
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - table
            - --table
            - gum2_setup_lead
  - triggers: [":gum"]
    label: "Plot seed: Location archetype"
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - table
            - --table
            - gum2_setup_location
  - triggers: [":gum"]
    label: "Plot seed: Mission"
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - table
            - --table
            - gum2_setup_mission
  - triggers: [":gum"]
    label: "Plot seed: Motivation"
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - table
            - --table
            - gum2_setup_motivation
  - triggers: [":gum"]
    label: "Plot seed: Opposition"
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - "%CONFIG%/scripts/playbtw_core"
            - table
            - --table
            - gum2_setup_opposition